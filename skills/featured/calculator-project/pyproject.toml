[project]
name = "calculator"
version = "0.1.0"
description = "Calculator — Edge AI Gallery skill: Python reference logic + tests"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
