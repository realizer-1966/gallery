[project]
name = "expense-splitter"
version = "0.1.0"
description = "Expense Splitter — Edge AI Gallery skill: Python reference logic + tests"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
